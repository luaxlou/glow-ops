## 1. Implementation
- [ ] 1.1 Remove `apply` command from `cmd/glow`.
- [ ] 1.2 Refactor `cmd/glow` to use `cobra` subcommands structure (`glow app start`, etc.).
- [ ] 1.3 Ensure all capabilities previously in `apply` are available via imperative commands.
